export const metadata = {
  title: "Your Cart | Zewa Feeds",
  description: "Review the items in your cart before checking out — premium insect-protein fish food, NABL lab-certified.",
};

export default function CartLayout({ children }) {
  return children;
}
